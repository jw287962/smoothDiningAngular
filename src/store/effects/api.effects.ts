import { Injectable } from '@angular/core';
import { createEffect } from '@ngrx/effects';

@Injectable()
export class ApiEffects {
  // loginAction$ = createEffect(()
  // => this.loginAction$.pipe(ofType('[')))
}
